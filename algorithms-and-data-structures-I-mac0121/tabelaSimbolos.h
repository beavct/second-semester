/* Tabela de símbolos para nós */
#include "arv.h"


no * busca (no * raiz, char * chave);
/* insere no com chave "x" e valor = valor */
no * insercao (no * raiz, char * chave, int valor);
no * remocao (no * raiz, char * chave);

