#include "aula19-par.h"

/*!
 * \brief Função: ehImpar
 * Verifica se um número é ímpar ou não. Faz isso com base na função
 * ehPar (faz 1 - o retorno daquela função já que o resultado precisa
 * ser o contrário)
 *
 * \param numero 1: (int) número que vai ter a imparidade testada
 * \return retorno: (int) 1 se for ímpar. 0 se não for ímpar.
 */
int ehImpar (int numero);
